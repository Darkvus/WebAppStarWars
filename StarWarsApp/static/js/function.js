// Fichero js personalizado.
$(document).ready(function(){
   // Clase para iniciar la datatable en html.
   $('.startDatatable').DataTable({
      // Para que por defecto no te ponga ordenada la datatable por ninguna columna.
      "order": []
   });
});